{
  "name": "file-registry-smart-contract",
  "version": "1.0.0",
  "main": "deploy.js",
  "scripts": {
    "deploy": "npx hardhat run deploy.js --network sepolia",
    "deploy:local": "npx hardhat run deploy.js --network ganache"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "dotenv": "^16.3.1",
    "ethers": "^5.7.2",
    "hardhat": "^2.20.2"
  }
} 